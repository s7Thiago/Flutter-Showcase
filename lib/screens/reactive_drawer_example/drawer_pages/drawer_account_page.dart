import 'package:flutter/material.dart';

class DrawerAccountPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Account Page',
        style: Theme.of(context).textTheme.body1,
      ),
    );
  }
}
