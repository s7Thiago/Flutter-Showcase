import 'package:flutter/material.dart';

class DrawerFavoritesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Favorites Page',
        style: Theme.of(context).textTheme.body1,
      ),
    );
  }
}
